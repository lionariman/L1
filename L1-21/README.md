# L1-21: Паттерн «Адаптер» (Adapter)

## Запуск:
```bash
go run main.go
```

## Что делает:
Реализует паттерн Adapter: адаптер приводит новый интерфейс логгера (`NewLogger`) к старой реализации (`OldLoggerStruct`), делегируя вызовы и проставляя тип сообщения (INFO).
